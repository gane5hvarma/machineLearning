package candidate;

import java.util.*;

class CandidateElimination{
    CandidateElimination(int i){
        // We have to do the number of times there are number of types.
        // Just put it in a for loop
        ArrayList<Hypothesis> hypothesis = Reader.read("zoo.data", i);
    }
}